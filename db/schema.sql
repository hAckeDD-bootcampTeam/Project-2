-- Using sequelize as ORM, so only need to set up the database which then populated with models

-- Drops the database if it exists currently --
DROP DATABASE IF EXISTS cacheIt;
-- Creates the database --
CREATE DATABASE cacheIt;

USE cacheIt; 